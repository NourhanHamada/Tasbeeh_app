import 'package:flutter/material.dart';

class AzkarDetailsLayout extends StatelessWidget {
   AzkarDetailsLayout({Key? key}) : super(key: key);

   final List<String> titles = [];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
